public interface Shape
{
    // Every shape must have "area" and "perimeter" methods, taking no
    //  arguments and returning a double. Each shape may implement these
    //  methods differently.
    double area();
    double perimeter();
}
